import { ValueAnimation } from "./ValueAnimation"

export class AsyncValueAnimation extends ValueAnimation {
    constructor() {
        super()
        /**
         * 1. Make animatable
         *    - We only need to resolve "auto"
         */
    }
}
